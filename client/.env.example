# Backend WebSocket URL
BACKEND_URL=ws://localhost:8000/ws

# RGB Camera ID (device path or index)
# Linux: /dev/video2 or /dev/video3 (whichever is RGB)
# Windows: 2 (typically)
CAMERA_ID=/dev/video2

# Depth Camera ID (device path or index)
# Linux: /dev/video1 (typically the depth stream)
# Windows: 0 or 1
# Set to empty or "none" to disable depth
DEPTH_CAMERA_ID=/dev/video1
