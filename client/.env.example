# Backend WebSocket URL
BACKEND_URL=ws://localhost:8000/ws

# RGB Camera ID (device path or index)
# Linux: /dev/video2 or /dev/video3 (whichever shows color)
# Windows: 2 (typically for Orbbec Astra Pro 2)
CAMERA_ID=/dev/video2

# IR/Depth disabled for stability
IR_CAMERA_ID=none
